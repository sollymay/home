class BlogPost {
  final String title;
  final String content;
  final DateTime createdOn;
  final DateTime updatedOn;
  // final List<Comment> comments;

  BlogPost(this.title, this.content, this.createdOn, this.updatedOn);
}
